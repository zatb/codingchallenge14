// Create a module that calculates the interest rate using the formula: Interest = Principal * Rate * Time.

export function calculateInterest(principal, rate, time) {
    return principal * rate * time;
}
